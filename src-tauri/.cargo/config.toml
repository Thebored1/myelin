[build]
# Cap parallel codegen so heavy linking (arrow/lance/tectonic) doesn't exhaust
# RAM and swap on 16 GB machines. Raise on boxes with more memory.
jobs = 6

[target.'cfg(windows)']
rustflags = ["-C", "target-feature=+crt-static"]